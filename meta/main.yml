---
galaxy_info:
  author: HurricaneHrndz
  role_name: rustup
  # Compliance with:
  #    https://galaxy.ansible.com/docs/contributing/namespaces.html#galaxy-namespace-limitations
  # Effectively one of the following is required:
  #   1. The author can be explicitly lowercased (and namespace omitted and implied)
  #   2. A lowercase namespace value can be explicitly specified, leaving the author as any case desired
  namespace: hurricanehrndz
  description: Installs rustup and rust toolchain.
  license: MIT
  min_ansible_version: 2.8

  platforms:
    - name: Fedora
      versions:
        - 30
    - name: Ubuntu
      versions:
        - bionic
    - name: Archlinux
      versions:
        - all
    - name: macOS
      versions:
        - all
  galaxy_tags: [rustup, rust, cargo]

dependencies: []
